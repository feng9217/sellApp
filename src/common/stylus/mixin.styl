// 函数式的css
// 1px实线 的问题, 在PC端看是 1px 但移动端像素密度高的机子, 说不定就会是 2px
// 解决方法就是 伪类 + DPR查询
border-1px-bottom($color)
  position: relative
  &:after
    // 伪类相对于元素本身做绝对定位
    display: block
    position: absolute
    left: 0
    bottom: 0
    width: 100%
    border-top: 1px solid $color
    content: ' '

border-1px-top($color)
  position: relative
  &:before
    // 伪类相对于元素本身做绝对定位
    display: block
    position: absolute
    left: 0
    top: 0
    width: 100%
    border-top: 1px solid $color
    content: ' '

// 带 DPR查询 的图片引用
bg-image($url)
  background-image: url($url + "@2x.png")
  @media(-webkit-min-device-pixel-ratio: 3),(min-device-pixel-ratio: 3)
    background-image: url($url + "@3x.png")
